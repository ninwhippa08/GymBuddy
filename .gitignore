# Editor / OS noise
.DS_Store
Thumbs.db
*.stackdump

# Local tooling artifacts, not part of the app
skills-lock.json
.claude/
.agents/

# Unrelated work that happens to live in this directory. This repo is PUBLIC
# and published to GitHub Pages, so anything untracked here is one careless
# `git add .` away from being on the internet.
.tmp/
*.pptx
